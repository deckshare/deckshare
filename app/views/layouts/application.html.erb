<!DOCTYPE html>
<html>
  <head>
    <title>Deckshare</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "energy", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <div>Deckshare</div>
      <div>
        <%= current_user? ? link_to("Log out", session_path, data: { turbo_method: :delete }) : link_to("Log in", new_session_path) %>
      </div>
    </header>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
